import React from 'react';

interface SaveBookProps {

  }

  const SaveBook: React.FC<SaveBookProps> = ({ book }) => {
    
    const handleSaveBook = async () => {
        try {
  




        } catch (error) {

            console.error("An error occurred", error)
        }
    }
}

